name: werkly_candidate_app
description: Candidate-facing Werkly mobile app for resumes, jobs, and applications.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^10.3.10
  http: ^1.5.0
  share_plus: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/werkly_logo.png
